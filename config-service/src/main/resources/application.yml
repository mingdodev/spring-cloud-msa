server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: file:///Users/minseojang/Desktop/project/spring-msa-config
          default-label: main
